# Variables d'environnement pour Java Cine API
# Copiez ce fichier vers .env et modifiez les valeurs

# TMDB API Token (obligatoire)
# Obtenez votre token sur https://www.themoviedb.org/settings/api
TMDB_BEARER_TOKEN=your_tmdb_bearer_token_here

# Base de données (optionnel, utilise les valeurs par défaut si non spécifié)
DATABASE_URL=jdbc:postgresql://localhost:5432/cine
DATABASE_USERNAME=postgres
DATABASE_PASSWORD=admin

# Configuration de l'application (optionnel)
SERVER_PORT=8080
SPRING_PROFILES_ACTIVE=dev

# Configuration Docker (utilisé par docker-compose)
POSTGRES_DB=cine
POSTGRES_USER=postgres
POSTGRES_PASSWORD=admin
